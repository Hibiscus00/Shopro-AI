version: '3.8'

services:
  shopro-mcp:
    build:
      context: .
      dockerfile: Dockerfile.mcp
    ports:
      - "8080:8080"
    env_file:
      - ../.env
    environment:
      - HOST=0.0.0.0
      - PORT=8080
    restart: unless-stopped
